import { HeaderContainer, HeaderBrand } from './Header.style'

const Header = () => {
  return <HeaderContainer>
    <HeaderBrand>Recipe Finder</HeaderBrand>
  </HeaderContainer>
}

export default Header